# Fixture inventory: the local machine pretending to be a Proxmox node.
# Overrides live in group_vars/all.yml next to this file.
[proxmox]
localhost ansible_connection=local

[proxmox:vars]
ansible_python_interpreter=/usr/bin/python3
