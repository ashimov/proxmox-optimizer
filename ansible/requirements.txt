# Control node requirements.
#
# netaddr is not optional: the ipaddr filter from ansible.utils needs it, and
# the networking, firewall and provider roles all use it.
ansible>=14.3.1,<15
netaddr>=0.9
